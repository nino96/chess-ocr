# GB10 is deliberately separate from CPU wheels.  Resolve with the NVIDIA
# PyTorch container/wheel index selected for the installed driver and record the
# image digest or wheel URL + SHA-256 in work/native/status.md before use.
# These application packages remain architecture-independent pins.
numpy==2.2.4
loguru==0.7.3
onnx==1.17.0
onnxruntime==1.20.1
opencv-python-headless==4.11.0.86
pillow==11.1.0
safetensors==0.5.3
timm==1.0.15
thop==0.1.1.post2209072238
tabulate==0.9.0
